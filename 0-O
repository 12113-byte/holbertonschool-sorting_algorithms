#include <stdio.h>

int main(void)
{
    FILE *f = fopen("0-O", "w");
    if (f == NULL)
    {
        perror("Error opening file");
        return 1;
    }

    fprintf(f, "O(n)\n");
    fprintf(f, "O(n^2)\n");
    fprintf(f, "O(n^2)\n");

    fclose(f);
    return 0;
}

